The honesty note. Syntax Tree states missing evidence out loud instead of hiding it.

```jsx
<Callout icon={<StatusBadge status="insufficient" />}>
  This node has architecture-map data, but no generated architectural explanation yet.
</Callout>
```

Copy names the limit plainly and never apologises. Use `unsupported` only when evidence contradicts a claim.
