Chromeless 30×30 icon button for chrome areas (top bar, panel close, menu toggle).

```jsx
<IconButton label="Close explanation" icon={<Icon name="x" size={17} />} />
```

Hover reveals a slate tint plus a faint border — never a filled color. Label is mandatory.
