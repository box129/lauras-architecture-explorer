Every enclosed area in Syntax Tree is this card: paper fill, 1px `--obs-border`, 12px radius, ink-tinted shadow.

```jsx
<Panel><h2>Start a scan</h2></Panel>
```

Never add colored left borders, gradients or heavier radii. Over the architecture map use `translucent`.
