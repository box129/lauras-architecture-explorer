Marks which kind of fact the user is looking at — the single most important rule in this product.

```jsx
<ProvenanceChip kind="cluster" />
<ProvenanceChip kind="ai" />
```

The AI chip is the only pill-shaped one and the only one on the iris accent; verification statuses never render through this component. Never swap a layer's icon or word.
