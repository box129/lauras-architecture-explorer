Key files for the selected area, in the rail's evidence tab.

```jsx
<FileList items={[{filePath:'app/orders/service.py', reason:'Primary file returned by the architecture map.'}]} />
```

Every row carries a reason — a bare file list is not evidence. Empty state states the fact plainly.
