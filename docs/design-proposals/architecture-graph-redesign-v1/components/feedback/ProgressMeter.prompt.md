Shows real counted progress — use it only when both numbers are real.

```jsx
<ProgressMeter value={412} total={1180} caption="syntax_tree_refurbished/app/architecture_map/projection.py" />
```

If the totals are unknown, show the quiet line "Preparing the analysis job." instead of a fake bar.
