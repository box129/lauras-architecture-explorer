Single toggle for an analysis flag.

```jsx
<CheckField checked={requireLlm} onChange={setRequireLlm}>Require live LLM explanations</CheckField>
```

Label reads as a plain statement of what will happen. Disable (never hide) it when another setting forces the value.
