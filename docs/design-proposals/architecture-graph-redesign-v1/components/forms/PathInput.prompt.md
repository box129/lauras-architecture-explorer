The 48px repository-path field from the entry screen.

```jsx
<PathInput value={path} onChange={setPath} onBrowse={() => setBrowserOpen(true)} />
```

Focus draws a slate border plus a 3px 12% ring. Keep both affordances: typed paths and the folder browser.
