Small field for machine values — model overrides, ids, paths.

```jsx
<TextInput value={model} onChange={setModel} placeholder="leave empty to use configured default" />
```

Placeholders are lowercase instructions, not example values dressed as data.
