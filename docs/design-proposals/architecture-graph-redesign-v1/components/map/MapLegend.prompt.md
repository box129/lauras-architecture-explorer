Explains the map's dots and edge kinds inline, bottom-left of the canvas.

```jsx
<MapLegend statuses={['verified','partial','inferred']} edges={['flow','inferred']} />
```

Solid slate edges are extractor-observed relations; dashed clay edges are inferred. Always show the legend when edges are visible.
