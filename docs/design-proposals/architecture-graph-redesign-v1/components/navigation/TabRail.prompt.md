The rail's reading-level switch: the same facts, told three ways.

```jsx
<TabRail value={tab} onChange={setTab} />
```

Keep the order simple → technical → evidence; it is a ramp from plain language to raw spans.
